import colors from "./colors";
import dimensions from "./dimensions";
import alignments from "./alignments";
import fonts from "./fonts";

// TODO: has to be define new colors, alignments, fonts, dimensions

const theme = {
  colors,
  dimensions,
  alignments,
  fonts,
};

export default theme;
